%nav.navbar.navbar-inverse{role: 'navigation'}
  .container
    .navbar-header
      = link_to root_path, class: 'navbar-brand' do
        = image_tag 'favicon.ico'
        Блог

    .navbar-collapse.collapse
      - if admin_signed_in?
        %ul.nav.navbar-nav
          %li.dropdown{role: 'presentation'}
            = link_to '#', class: 'dropdown-toggle', 'data-toggle': 'dropdown' do
              Справочники
              %span.caret
            %ul.dropdown-menu
              %li= link_to 'Введение в Rspec', page_path('introduction_to_the_tests')
              %li= link_to 'Интеграционные тесты', page_path('acceptance')

      = render partial: 'layouts/admin_bar'
