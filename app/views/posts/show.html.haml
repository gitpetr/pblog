.row
  .col-md-12
    %h1.post-title= @post.title
    .post-image
      = image_tag @post.image.url(:medium), class: 'img-responsive center-block' if @post.image?

    %p= @post.body.html_safe
    = render 'discus'
